import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  dropdown: {
    backgroundColor: 'red',
    borderRadius: 8,
    paddingVertical: 13,
    paddingHorizontal: 12,
  },
});
